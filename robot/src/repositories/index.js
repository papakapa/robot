const { HostRepository } = require('./host');
const { LinkRepository } = require('./links');
const { IndexesRepository } = require('./indexes');

module.exports = { HostRepository, LinkRepository, IndexesRepository };
